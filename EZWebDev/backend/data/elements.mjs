import Element from '../element.mjs';

const elements = [
    new Element(12, 3, 5, 6, "Wow this button is cool"),
    new Element(25, 15, 8, 9, "Who knew web dev is so fun?")
];

export default elements;